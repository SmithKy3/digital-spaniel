import * as React from 'react';
import { RecentProjects } from './RecentProjects';

export const Work: React.FC<{}> = () => {
	return (
		<section id="work">
			<RecentProjects />
		</section>
	);
};
